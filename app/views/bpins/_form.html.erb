<%= form_with(model: bpin) do |form| %>
  <% if bpin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bpin.errors.count, "error") %> prohibited this bpin from being saved:</h2>

      <ul>
        <% bpin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :board_id %>
    <%= form.number_field :board_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
