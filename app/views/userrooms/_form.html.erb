<%= form_with(model: userroom) do |form| %>
  <% if userroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(userroom.errors.count, "error") %> prohibited this userroom from being saved:</h2>

      <ul>
        <% userroom.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :room_id %>
    <%= form.number_field :room_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
