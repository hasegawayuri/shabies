<%= form_with(model: member) do |form| %>
  <% if member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(member.errors.count, "error") %> prohibited this member from being saved:</h2>

      <ul>
        <% member.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :user_id , value: current_user.id , value: session[:user_link] %>
  </div>

  <div class="field">
    <%= form.hidden_field :group_id , value: session[:group_member] %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
